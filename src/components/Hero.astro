---
import type { Information } from '../types';
import Contact from './Contact.astro';

interface Props {
	data: Information;
}

const { data } = Astro.props;
---

<div
	class='min-h-max w-full flex items-center justify-between my-4 flex-wrap lg:flex-nowrap'
>
	<div class='w-full lg:w-auto'>
		<span>Soy</span>
		<h1 class='text-3xl font-bold'>{`${data.name} ${data.lastname}`}</h1>
		<p class='mt-3 mb-10'>
			Amante del aprendizaje contínuo cuyo propósito es hacer lo que más me
			apasiona: <code
				class='bg-gray-300 dark:bg-gray-600 p-1 rounded font-bold inline-block'
				>Programar</code
			>
		</p>
		<Contact />
	</div>
	<div class='h-full mt-6 lg:mt-0'>
		<img src='/images/hero.png' alt='Hero image' />
		<div class='space-x-3 text-right font-bold'>
			<span>{data.country} 🇵🇪</span>
			<span>Desarrollador web</span>
		</div>
	</div>
</div>
