---
interface Props {
	href: string;
	label: string;
}

const { href, label } = Astro.props;
---

<li>
	<a
		class='hover:bg-gray-300 dark:hover:bg-gray-600 rounded px-3 py-2'
		href={href}>{label}</a
	>
</li>
